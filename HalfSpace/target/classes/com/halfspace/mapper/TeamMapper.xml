<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.halfspace.mapper.TeamMapper">

<!-- 
	<select id="teamDetail" resultType="com.halfspace.persistence.TeamVO">
		SELECT * FROM team_tbl WHERE tno = #{tno}
	</select>
	
	<insert id="teamCreate">
		INSERT ALL
			INTO teamlist(listno, name, coach)
				VALUES (teamlist_num.nextval, #{name}, #{coach})
			INTO team_tbl(tno, name, coach, logo, intro)
				VALUES (team_num.nextval, #{name}, #{coach}, #{logo}, #{intro})
			SELECT * FROM DUAL
	</insert>
 	
 	<update id="teamUpdate">
 		UPDATE team_tbl
 			SET
 		name = #{name}, coach = #{coach}, logo = #{logo}, intro = #{intro}
 			WHERE tno = #{tno}
 	</update>
 	

 	<delete id="teamDelete">
 		DELETE FROM team_tbl
 			WHERE tno = #{tno}
 	</delete>
 	
 	<select id="getTeamListCnt" resultType="long">
 		SELECT count(*) FROM teamlist WHERE
 			<include refid="search"></include>
 		listno > 0
 	</select>
 -->
</mapper>